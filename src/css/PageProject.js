import styled from "styled-components";

const css = styled.div`
  main {
    display: flex;
    justify-content: space-evenly;
    flex-flow: row wrap;
    padding: 48px 30px;
  }
`

export default css;
